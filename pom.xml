<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <!-- Maven model version -->
    <modelVersion>4.0.0</modelVersion>

    <!-- Unique organization or company -->
    <groupId>org.example</groupId>

    <!-- Project name -->
    <artifactId>demo-project</artifactId>

    <!-- Version of artifact Default? -SNAPSHOT -->
    <version>2.3-SNAPSHOT</version>

    <!-- Packaging type -->
    <packaging>jar</packaging>

    <!-- Project name -->
    <name>Demo Maven Project</name>

    <!-- Global properties -->
    <properties>
        <!-- Java version used for compilation -->
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
    </properties>

    <!-- External libraries -->
    <dependencies>

        <!-- JUnit for testing -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>5.10.2</version>
            <scope>test</scope>
        </dependency>

    </dependencies>

    <!-- Build configuration -->
    <build>

        <plugins>

            <!-- Java compiler plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
            </plugin>

            <!-- Test execution plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
            </plugin>
        </plugins>

    </build>

    <distributionManagement>
        <repository>
            <id>l3c-auto-88-10.162.120.164-repo22</id>
            <url>http://10.162.120.164:8081/repository/maven-snapshots</url>
        </repository>
    </distributionManagement>
</project>